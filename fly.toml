app = "james-dogfood"
primary_region = "iad"

[build]
  dockerfile = "Dockerfile"

[env]
  JAMES_DIR = "/data"
  JAMES_DEPLOYMENT = "docker"

[http_service]
  internal_port = 19898
  force_https = true
  auto_stop_machines = "off"
  auto_start_machines = true
  min_machines_running = 1

[[vm]]
  size = "shared-cpu-2x"
  memory = "1gb"

[mounts]
  source = "james_data"
  destination = "/data"

[checks]
  [checks.health]
    type = "http"
    port = 19898
    path = "/api/health"
    interval = "30s"
    timeout = "5s"
